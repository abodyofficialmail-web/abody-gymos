name: Sync Ueno Shifts 2026-10

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "true でDB更新しない"
        required: false
        default: true
        type: boolean
      sync_db:
        description: "true で本番DBへ反映（confirmed・予約枠解放）"
        required: false
        default: false
        type: boolean

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm ci --legacy-peer-deps
      - name: Plan / sync Ueno October shifts
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          set -euo pipefail
          export NEXT_PUBLIC_SUPABASE_URL="$(printf '%s' "$NEXT_PUBLIC_SUPABASE_URL" | tr -d '\r\n')"
          export SUPABASE_SERVICE_ROLE_KEY="$(printf '%s' "$SUPABASE_SERVICE_ROLE_KEY" | tr -d '\r\n')"
          ARGS=()
          if [ "${{ github.event.inputs.dry_run }}" = "true" ] || [ "${{ github.event.inputs.sync_db }}" != "true" ]; then
            ARGS+=(--dry-run)
          fi
          node scripts/sync-ueno-shifts-2026-10.mjs "${ARGS[@]}"
